# Spoolrail documentation

The Spoolrail documentation site uses Astro, Starlight, and `starlight-theme-black`.

## Local development

Use Node.js 22.12 or later.

```bash
npm install
npm run dev
```

Documentation pages live in `src/content/docs`. Run `npm run build` for a production build. Check formatting with `npm run format:check`.
